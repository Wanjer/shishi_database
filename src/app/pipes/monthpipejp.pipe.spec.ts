import { MonthpipejpPipe } from './monthpipejp.pipe';

describe('MonthpipejpPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthpipejpPipe();
    expect(pipe).toBeTruthy();
  });
});
