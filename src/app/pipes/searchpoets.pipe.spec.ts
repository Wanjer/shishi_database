import { SearchpoetsPipe } from './searchpoets.pipe';

describe('SearchpoetsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchpoetsPipe();
    expect(pipe).toBeTruthy();
  });
});
