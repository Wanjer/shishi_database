import { PoetworksearchPipe } from './poetworksearch.pipe';

describe('PoetworksearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PoetworksearchPipe();
    expect(pipe).toBeTruthy();
  });
});
