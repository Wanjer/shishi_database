import { SplitslashPipe } from './splitslash.pipe';

describe('SplitslashPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitslashPipe();
    expect(pipe).toBeTruthy();
  });
});
