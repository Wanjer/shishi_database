import { BibliopipePipe } from './bibliopipe.pipe';

describe('BibliopipePipe', () => {
  it('create an instance', () => {
    const pipe = new BibliopipePipe();
    expect(pipe).toBeTruthy();
  });
});
