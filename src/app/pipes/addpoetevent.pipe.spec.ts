import { AddpoeteventPipe } from '../pipes/addpoetevent.pipe';

describe('AddpoeteventPipe', () => {
  it('create an instance', () => {
    const pipe = new AddpoeteventPipe();
    expect(pipe).toBeTruthy();
  });
});
